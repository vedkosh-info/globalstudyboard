# Copy this file to image-pipeline/.env and paste your real key after the = sign.
#   cp image-pipeline/.env.example image-pipeline/.env
# image-pipeline/.env is gitignored and can never be committed.
# Only GEMINI_API_KEY is required. The other two are optional 3-way bake-off extras.

GEMINI_API_KEY=
# FAL_KEY=
# OPENAI_API_KEY=
