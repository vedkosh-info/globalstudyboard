# Never upload build artefacts or dev tooling to Vercel.
# Vercel installs fresh node_modules and builds .next itself.
node_modules
.next
.git
.env.local
.env*.local
*.tsbuildinfo

# Android (TWA) wrapper — built by Gradle, never by Vercel.
android-app
gradle
gradlew
gradlew.bat
build.gradle
settings.gradle
gradle.properties
